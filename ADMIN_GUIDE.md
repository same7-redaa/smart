# نظام إدارة محتوى معارض الأعمال

## 📋 المميزات

### ✅ تم التنفيذ:

1. **لوحة تحكم كاملة (Admin Panel)**
   - إضافة مشاريع جديدة
   - تعديل المشاريع الموجودة
   - حذف المشاريع
   - عرض جميع المشاريع في جدول منظم

2. **دعم الوسائط المتعددة**
   - 🖼️ **الصور**: دعم روابط Google Drive مع تحويل تلقائي للروابط المباشرة
   - 🎥 **الفيديو**: دعم روابط YouTube مع تحويل تلقائي لروابط التضمين
   - يتم التعرف تلقائياً على نوع الوسائط

3. **قاعدة بيانات Firebase**
   - تخزين آمن في Firestore
   - عمليات CRUD كاملة
   - بيانات ديناميكية في الوقت الفعلي

4. **حماية بنظام تسجيل الدخول**
   - Firebase Authentication
   - صفحة تسجيل دخول احترافية
   - حماية لوحة التحكم

5. **واجهة مستخدم احترافية**
   - تصميم عصري ومتجاوب
   - رسائل تأكيد للعمليات
   - مؤشر تحميل
   - جداول منظمة

## 🚀 كيفية الاستخدام

### الوصول للوحة التحكم:

افتح Console في المتصفح (F12) واكتب:
```javascript
window.openAdmin()
```

### إضافة مستخدم Admin في Firebase:

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك `smart-95d4a`
3. من القائمة الجانبية اختر **Authentication**
4. اضغط على **Get Started** (إذا لم تكن قد فعّلت Authentication)
5. فعّل **Email/Password** من تبويب Sign-in method
6. اضغط على **Add User**
7. أدخل البريد الإلكتروني وكلمة المرور

### إعداد قاعدة البيانات:

1. في Firebase Console، اختر **Firestore Database**
2. اضغط **Create database**
3. اختر **Start in test mode** (للتطوير)
4. اضغ **Next** ثم **Enable**

### قواعد Firestore الموصى بها (بعد التطوير):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // مشاريع معرض الأعمال الرئيسي
    match /projects/{projectId} {
      allow read: if true; // الجميع يمكنهم القراءة
      allow write: if request.auth != null; // فقط المستخدمين المسجلين يمكنهم الكتابة
    }
    
    // المشاريع المميزة في الصفحة الرئيسية
    match /featuredProjects/{projectId} {
      allow read: if true; // الجميع يمكنهم القراءة
      allow write: if request.auth != null; // فقط المستخدمين المسجلين يمكنهم الكتابة
    }
    
    // إعدادات الموقع (صورة خلفية Cta)
    match /settings/{settingId} {
      allow read: if true; // الجميع يمكنهم القراءة
      allow write: if request.auth != null; // فقط المستخدمين المسجلين يمكنهم الكتابة
    }
  }
}
```

## 📝 إضافة مشروع

1. افتح لوحة التحكم
2. اضغط "إضافة مشروع جديد"
3. املأ النموذج:
   - **الخدمة**: اختر من القائمة
   - **عنوان المشروع**: اسم المشروع
   - **الوصف**: وصف اختياري
   - **رابط الوسائط**: 
     - لـ Google Drive: الصق الرابط العادي (سيتم التحويل تلقائياً)
     - لـ YouTube: الصق أي شكل من روابط YouTube
   - **التصنيف**: اختياري (مثل: مطاعم، عقارات، إلخ)

## 🔧 محول روابط Google Drive

الكود يحول تلقائياً الروابط من:
```
https://drive.google.com/file/d/FILE_ID/view?usp=sharing
```

إلى:
```
https://drive.google.com/uc?export=view&id=FILE_ID
```

## 🎬 دعم روابط YouTube

يدعم جميع أشكال روابط YouTube:
- `https://www.youtube.com/watch?v=VIDEO_ID`
- `https://youtu.be/VIDEO_ID`
- `https://www.youtube.com/embed/VIDEO_ID`

## 📂 الملفات المضافة

- `firebase.config.ts` - إعداد Firebase
- `utils/mediaUtils.ts` - محولات الروابط
- `services/projectService.ts` - خدمات CRUD
- `components/AdminPanel.tsx` - لوحة التحكم
- `components/ProtectedAdminPanel.tsx` - حماية لوحة التحكم
- تحديثات على `App.tsx` و `ServicePortfolioPage.tsx`

## 🎨 الميزات الإضافية

- تصميم متجاوب لجميع الشاشات
- تأثيرات hover احترافية
- رسائل تأكيد للعمليات
- معالجة الأخطاء
- مؤشرات التحميل

---

**تم التنفيذ بنجاح! 🎉**
